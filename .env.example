# 只在你启用 --llm 时需要
OPENAI_API_KEY=
OPENAI_MODEL=
# 可选：自定义 OpenAI Base URL（比如代理/兼容服务；支持填 https://xxx 或 https://xxx/v1）
OPENAI_BASE_URL=https://api.openai.com

# Embeddings（用于 memory 向量检索；OpenAI-compatible /v1/embeddings）
# ⚠️ 隐私口径：只有你显式配置了 EMBEDDINGS_API_KEY 才会启用向量索引/检索（避免误把记忆文本出网）。
EMBEDDINGS_API_KEY=
# 例：text-embedding-3-small / bge-m3 / 你代理里支持的 embedding 模型名
EMBEDDINGS_MODEL=text-embedding-3-small
# 可选：自定义 embeddings base url（支持填 https://xxx 或 https://xxx/v1）
EMBEDDINGS_BASE_URL=https://api.openai.com
# 可选：额外 headers（JSON 对象字符串），用于 org/project/custom auth 等
EMBEDDINGS_HEADERS_JSON=

# Gemini（Google AI Studio / 代理）
GEMINI_API_KEY=
# 例如：gemini-2.0-flash / gemini-1.5-flash / gemini-2.5-pro（以你账号可用为准）
GEMINI_MODEL=
# 默认走 Google AI Studio 的 Gemini API；如果你用中转站/代理，这里填你的 base url（需要包含 /v1beta）
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
# 认证方式：x-goog-api-key（默认，AI Studio） / authorization（中转站常见：Authorization: Bearer <key>）
GEMINI_API_KEY_MODE=x-goog-api-key

# TuShare（可选：用于北向/ETF份额规模等数据；不启用不影响主流程）
TUSHARE_TOKEN=
# ⚠️ 非官方代理/转发有合规与安全风险（你自己确认后再用）
TUSHARE_HTTP_URL=

# 行情数据源偏好（可选；未设则默认 AkShare）
# 可选值：akshare / tushare / auto（auto=优先 TuShare，失败回退 AkShare）
# 全局默认：
# LLM_TRADING_PRICE_SOURCE=auto
# 按资产覆盖（推荐仅对 A股 stock/index 设 auto，ETF 若要复权连续性可保持 AkShare）：
# LLM_TRADING_PRICE_SOURCE_STOCK=auto
# LLM_TRADING_PRICE_SOURCE_INDEX=auto
# LLM_TRADING_PRICE_SOURCE_ETF=akshare
